name: Publish Main Release

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  publish:
    uses: StarCustomChat/StarCustomChat/.github/workflows/publish.yml@main
    secrets: inherit
    with:
      create_release: true
      send_discord: true
      upload_to_google_drive: true
